OUTTACOUCH

1) Project Overview

OUTTACOUCH is an event-first social connection platform designed to translate online discovery into offline relationships.
The core problem is that traditional social apps prioritize engagement loops (feeds, likes, comments) instead of facilitating real-world interactions.
OUTTACOUCH exists to make event attendance the primary signal of identity and trust, so connections form around shared experiences rather than superficial profile activity.

Why this project exists:
- People want meaningful connections but lack a reliable way to meet others around common interests.
- Hosts need a streamlined way to list events, manage attendance, and validate entry.
- Existing social graphs are noisy and do not reflect real-world context.

Real-world use cases:
- A user discovers a nearby music event, commits to attend, and later connects with other attendees.
- A host creates an event (free or paid), manages capacity, and validates entry with QR scans.
- A user builds a calendar-first profile that shows attended and hosted events as their social identity.
- A user invites phone contacts via SMS or WhatsApp using a referral link; when the contact signs up, they are automatically connected.

Target users:
- Attendees who want to meet new people through shared events.
- Hosts who need tools for event publishing, attendee management, and check-ins.
- Communities that prefer real-world trust signals over follower counts.

Key product promises:
- Relevance: discovery is ranked by location, timing, and interest match.
- Trust: connections are anchored in shared attendance.
- Action: the UX pushes users from browsing to committing and showing up.


2) System Design (High-Level + Low-Level)

High-level architecture is a single Next.js 15 application that serves both the frontend and the backend API.
The frontend uses the App Router with route groups: app/(auth) for authentication and onboarding flows, and app/(main) for the protected core product (explore, events, chat, profile, settings, notifications).
The backend is implemented as Next.js API routes in app/api, and a Socket.io server bootstrapped through pages/api/socketio.ts for realtime chat.
Core services: PostgreSQL via Prisma 7 ORM, Supabase Storage (image uploads), Twilio Verify (phone OTP), Twilio SMS/WhatsApp (invitations), SMTP/Nodemailer (email OTP and notifications), and qrcode.react (client-side QR rendering).
Payments: no online payment processing. Paid events show a "contact the host directly" message. Tickets are still tracked and QR-validated for entry using scannable QR codes on the tickets page.

Low-level design splits features into modules: authentication, event discovery and management, ticketing, connections, chat, memories, notifications, referrals/contacts, and storage.
Each module has API endpoints that map directly to UI screens and actions. Data access flows through Prisma queries with explicit ownership checks at the endpoint level.
Realtime messaging uses Socket.io rooms keyed by connectionId.
Notification dispatch runs as a Vercel Cron job every 5 minutes, hitting GET /api/notifications/dispatch secured by CRON_SECRET.

Data flow in a typical request:
Client action -> App Router page -> fetch to app/api endpoint -> JWT validation -> Prisma query -> JSON response -> UI state update.

Scalability considerations:
- API routes are stateless and scale horizontally.
- Prisma uses pooled DB connections (PgBouncer via Supabase); DIRECT_URL is used for migrations to bypass the pooler.
- Event feed is capped to 50 results and scored in memory.
- Socket.io is single-node and would require a Redis adapter for multi-node scaling.
- Supabase Storage is scalable for media.

Trade-offs:
- Monolithic Next.js app simplifies development and deployment but couples API and UI and requires extra work for realtime scaling.
- Event-first connections improve relevance and safety but reduce the speed of discovery compared to follower-based models.
- No online payments keeps the system simpler and avoids PCI scope, but limits monetization.

System overview (text diagram):
Client (Next.js App Router UI)
  -> API routes (app/api) for auth, events, tickets, chat, connections, notifications, referrals
  -> Prisma 7 ORM -> PostgreSQL (Supabase, pooled via PgBouncer)
  -> Supabase Storage for images
  -> Twilio Verify for OTP / Twilio SMS+WhatsApp for invitations
  -> Nodemailer/SMTP for email OTP and notifications
Realtime:
  Client <-> Socket.io server (pages/api/socketio.ts) <-> globalThis.io emitToRoom (lib/socketServer.ts)
Cron:
  Vercel Cron -> GET /api/notifications/dispatch (every 5 minutes)


3) Tech Stack and Design Choices

Frontend:
- Next.js 15 App Router + React 18: unified routing, server/client component split, API colocation.
- TypeScript: type-safe props, API payloads, and shared types across client and server.
- Tailwind CSS with custom design tokens: ink, parchment, ocean, accent, bg, subtle, border.
- Framer Motion: swipe and card interactions for event discovery.
- Leaflet (react-leaflet): interactive maps for event location and picker.
- Recharts: host analytics charts.
- react-hook-form: efficient form state and validation.
- Zustand: minimal global state (authStore, uiStore).

Backend:
- Next.js API routes: colocated API for rapid development and deployment.
- NextAuth v4 Credentials + JWT: session management via JWT, getToken in API routes, getServerSession in some routes.
- Prisma 7 with PrismaPg adapter: typed queries, migrations (using DIRECT_URL to bypass pooler), and transactional integrity.
- PostgreSQL (Supabase): relational model fits events, tickets, and connections.
- Socket.io: realtime chat, typing indicators, read receipts.

Integrations:
- Supabase Storage (admin key, server-side): scalable public media storage.
- Twilio Verify: phone OTP for signup and password reset.
- Twilio SMS + WhatsApp: referral invitation delivery.
- Nodemailer/SMTP: email OTP and notification delivery.
- Web Contact Picker API (navigator.contacts): device contact sync on supported mobile browsers.
- qrcode.react: client-side QR code rendering for ticket validation.

Design choices:
- Monolithic Next.js app for faster iteration and simpler deployment.
- App Router for core UI; pages/api used only to bootstrap Socket.io (App Router does not support persistent connections).
- JWT claims (profileComplete, isDeactivated) enforce onboarding and access at the middleware level.
- Server-side Supabase uploads avoid exposing storage service credentials to the client.
- No online payments: removes PCI scope and Stripe dependency; paid events route hosts and attendees to settle directly.
- Vercel Cron for notification dispatch: no separate worker process needed.


4) Database Design

Schema uses a relational model in PostgreSQL managed by Prisma 7.
The datasource URL is a PgBouncer pooled connection string (DATABASE_URL) for runtime, and a direct connection string (DIRECT_URL) in prisma.config.ts for migrations.

Models:

User
  - id, email, phone, passwordHash, displayName, bio, profilePhotoUrl
  - city, lat, lng (location for discovery scoring)
  - preferences (string array of interest categories)
  - profileComplete, isDeactivated
  - visibility (Visibility enum), approvalMode (ApprovalMode enum)
  - Relations: hosts Events, EventAttendees, Tickets, Connections (sent/received), Messages, Notifications, NotificationSchedules, OtpTokens, Memories, EventSwipes, ContactImports, ContactInvitations, ReferralLinks

Event
  - id, title, description, category, location (string address), lat, lng
  - startAt, endAt, maxAttendees, currentAttendees
  - isFree, price (numeric), currency
  - status (EventStatus enum), approvalMode
  - hostId -> User
  - Relations: EventImages, EventAttendees, NotificationSchedules, Tickets, shared Connections, Memories, EventSwipes

EventImage
  - id, eventId, url, order, isCover

EventAttendee
  - id, eventId, userId, status (AttendeeStatus enum)
  - Unique index on (eventId, userId)

Ticket
  - id, eventId, userId, eventAttendeeId (optional link)
  - paymentStatus (PaymentStatus enum)
  - qrCode (unique), validatedAt
  - quantity, amount, currency

Connection
  - id, user1Id, user2Id, status (ConnectionStatus enum)
  - sharedEventId (optional, for event-based connections)
  - requestedAt, acceptedAt

Message
  - id, connectionId, senderId, content, type (MessageType enum)
  - sentAt, readAt

Notification
  - id, userId, title, body, link
  - createdAt, readAt (null = unread)

NotificationSchedule
  - id, userId, eventId (optional), type, sendAt
  - sent (boolean), createdAt
  - Unique index on (userId, eventId, type, sendAt)

OtpToken
  - id, contact, otp (hashed for email), purpose (signup/reset)
  - verifiedAt, usedAt, expiresAt, userId (optional)
  - Index on (contact, purpose, usedAt)

Memory
  - id, userId, eventId, imageUrl, caption, createdAt

EventSwipe
  - id, userId, eventId, direction (like/pass), createdAt

ContactImport
  - id, userId, name, phone, status (pending/invited/registered)
  - invitedAt, registeredAt, registeredUserId
  - Unique on (userId, phone)

ContactInvitation
  - id, fromUserId, toPhone, channel (sms/whatsapp)
  - status (pending/sent/clicked/registered)
  - referralCode (unique, used as the join link code)
  - sentAt, clickedAt, registeredUserId

ReferralLink
  - id, code (unique), fromUserId, type (contact/general)
  - clicks, registrations, expiresAt

Enums:
- Visibility: private, connections, public
- ApprovalMode: auto, manual
- EventStatus: upcoming, ongoing, past, cancelled
- AttendeeStatus: committed, attended, missed, waitlisted
- ConnectionStatus: pending, accepted, declined, removed
- MessageType: text, event_card, location
- PaymentStatus: pending, paid, refunded, failed

Indexing:
- Unique on user.email, user.phone
- Unique on ticket.qrCode
- Unique on (eventId, userId) in EventAttendee
- OtpToken indexed on (contact, purpose, usedAt)
- NotificationSchedule unique on (userId, eventId, type, sendAt)
- ContactImport unique on (userId, phone)
- ContactInvitation indexed on referralCode (unique), fromUserId, toPhone
- ReferralLink indexed on code (unique), fromUserId

Data lifecycle:
- Deactivated users keep their row but email/phone are cleared for re-signup safety.
- Event deletion cascades to attendees, tickets, swipes, images, and memories links.
- OTP tokens expire and are marked used/verified as part of each auth flow.
- ContactInvitation progresses: pending -> sent -> clicked -> registered.
- When a referred user registers, a Connection is auto-created with status "accepted" and both users receive a welcome Notification.

Query optimization:
- Event feed caps to 50 rows with computed scoring (distance, preferences, timing, popularity).
- Connection suggestions filter on existing relationships and limit results.
- Notifications limited to 50 by createdAt desc.
- Transactions used for ticket creation and attendance updates to prevent race conditions.
- Prisma select used to avoid over-fetching columns.


5) Backend Deep Dive

API style: REST endpoints under app/api. All request/response bodies are JSON except storage upload (multipart/form-data).

Authentication endpoints:
- POST /api/auth/send-otp { contact, type, purpose } -> { status }
  Sends email OTP (Nodemailer) or phone OTP (Twilio Verify) for signup or reset.
- POST /api/auth/verify-otp { contact, otp, purpose } -> { token }
  Verifies the OTP; returns a signed token for use in the register/reset step.
- POST /api/auth/register { contact, password, token, ref? } -> { status }
  Creates the user, marks OTP used; if ref is a valid ContactInvitation.referralCode, completes referral: marks invitation "registered", updates ContactImport, creates Connection (accepted), sends welcome Notifications to both parties, increments ReferralLink.registrations.
- POST /api/auth/reset-password { contact, password, token } -> { status }

Event endpoints:
- GET /api/events -> scored feed of up to 50 events
- POST /api/events { title, category, location, lat, lng, startAt, endAt, maxAttendees, isFree, price, approvalMode } -> { id }
- GET /api/events/:id -> full detail with host, images, attendee count, analytics (host only)
- PATCH /api/events/:id -> update event (host only)
- DELETE /api/events/:id -> delete event and cascade cleanup (host only)
- POST /api/events/:id/commit -> create EventAttendee in a transaction, increment currentAttendees
- GET /api/events/host -> hosted events for current user
- POST /api/events/swipe { eventId, direction } -> record EventSwipe
- POST/DELETE /api/events/:id/images -> upload or delete event gallery images (Supabase Storage, server-side)

Ticket endpoints:
- GET /api/tickets/me -> user's tickets
- POST /api/tickets { eventId } -> create ticket (status pending; host confirms payment out-of-band)
- GET /api/tickets/:id/qr -> QR code for entry validation
- POST /api/tickets/validate { qr_code, event_id } -> validate QR at entry (host scanner)
- POST /api/tickets/refund { ticketId } -> cancel ticket if within 48h window, set paymentStatus "refunded", decrement capacity

Connection endpoints:
- GET /api/connections -> accepted connections
- GET /api/connections/discover -> users near current user
- GET /api/connections/suggestions -> users with shared event attendance
- GET /api/connections/requests -> pending incoming requests
- POST /api/connections/request/:userId -> send connection request
- GET/DELETE /api/connections/:id -> get or remove connection
- PUT /api/connections/:id/accept -> accept request
- PUT /api/connections/:id/decline -> decline request

Chat endpoints:
- GET /api/chat -> conversation list with last message and unread count
- GET /api/chat/:connectionId -> message history (only connection members)
- POST /api/chat/:connectionId { content, type } -> send message; emits via Socket.io
- PUT /api/chat/:connectionId/read -> mark messages read

Notification endpoints:
- GET /api/notifications -> list of notifications (newest 50)
- PUT /api/notifications/:id/read -> mark single notification read
- PUT /api/notifications/read-all -> mark all read
- GET /api/notifications/dispatch -> Vercel Cron handler (also accepts POST); finds pending NotificationSchedules, sends email/SMS, marks sent
- POST /api/notifications/subscribe -> stub for future push notification device token registration

Referral endpoints:
- GET /api/referrals -> stats + invitations list + referral links for current user
- POST /api/referrals/share { contactIds?, phones?, channel } -> sends SMS/WhatsApp invitation via Twilio; creates ContactInvitation + ReferralLink per contact
- GET /api/referrals/:code -> validates referral code, returns referrer info, tracks click; used by /join page

Contact endpoints:
- GET /api/contacts/sync -> returns ContactImport list + stats (registered/invited/pending)
- POST /api/contacts/sync { contacts: [{name, phone}] } -> upserts ContactImport rows; checks which phones are already registered users

User endpoints:
- GET/PATCH /api/users/me -> get or update current user profile
- PATCH /api/users/me/location { lat, lng, city } -> update location
- PATCH /api/users/me/privacy { visibility } -> update visibility
- GET /api/users/:id -> get public profile (visibility-gated)

Storage endpoint:
- POST /api/storage/upload (multipart) -> uploads to Supabase Storage via admin key, returns public URL

Authorization and security:
- NextAuth JWT validated in all protected API routes via getToken.
- Ownership enforced at endpoint level (event edit/delete checks hostId, chat access checks connection membership, ticket validation checks host role).
- Middleware redirects unauthenticated users to /login, incomplete profiles to /onboarding/location, deactivated users to /deactivated.
- Notification dispatch protected by dual auth: NOTIFICATION_DISPATCH_SECRET header or Vercel CRON_SECRET Bearer token.
- OTP resend cooldown enforced by checking latest OTP timestamp; Twilio Verify adds server-side rate limits.
- Supabase service role key never leaves the server.

Error handling:
- API routes return JSON { error: string } with status codes.
- Common statuses: 400 bad request, 401 unauthorized, 403 forbidden, 404 not found, 409 conflict, 410 gone (deprecated endpoints), 500 server error.
- Referral completion in /api/auth/register is wrapped in try/catch so a referral failure never blocks account creation.


6) Frontend Deep Dive

Component architecture:
- UI primitives in components/ui: PageShell (page wrapper with back button and subtitle), SectionCard (card with title/description), NotificationBell (unread badge + link to /notifications).
- Feature components in components/events: EventMap (Leaflet embed), MapPicker (interactive location selector), SwipeStack (card swipe for discovery).
- Profile components in components/profile: ProfileHeader, CalendarGrid (event calendar view), MemoriesGrid (photo grid).

Design system tokens (Tailwind):
- ink: primary text / buttons
- parchment: primary button text / soft backgrounds
- ocean: brand accent for tags and active states
- accent: secondary highlight
- bg: page background
- subtle: soft backgrounds
- border: neutral border color

Route structure:
- / (home) -> marketing/landing or redirect to /explore if authenticated
- /join?ref=CODE -> referral landing page; validates code via GET /api/referrals/:code, shows referrer info, links to /signup?ref=CODE
- (auth) group: /login, /signup, /signup/verify, /signup/password, /reset, /reset/verify, /reset/password, /onboarding/profile, /onboarding/location
- (main) group: /explore, /events/[id], /events/new, /events/manage, /events/manage/[id], /events/manage/[id]/edit, /events/manage/[id]/scanner, /connections, /chat, /chat/[id], /notifications, /profile, /profile/edit, /profile/calendar, /profile/memories, /profile/tickets, /settings, /settings/contacts, /settings/host, /settings/notifications, /settings/payments, /settings/privacy, /users/[id]

State management:
- Component-local state for forms, lists, and loading flags.
- Zustand authStore and uiStore for cross-component flags.
- No server state library for most features; simple fetch-on-mount pattern.
- @tanstack/react-query is available but not yet wired globally.

Signup flow with referral threading:
1. /join?ref=CODE -> validates code, links to /signup?ref=CODE
2. /signup reads ref from searchParams, appends &ref=CODE to verify redirect
3. /signup/verify reads ref, appends to password redirect
4. /signup/password reads ref, includes ref in register POST body
5. /api/auth/register reads ref, completes referral after user creation (non-fatal)
6. User is signed in and redirected to /onboarding/location

Notification bell:
- NotificationBell in (main)/layout.tsx sidebar header and mobile nav.
- Fetches GET /api/notifications on mount, counts items where readAt is null, shows badge (max "9+").
- Links to /notifications for full list.

Contact sync and invite (/settings/contacts):
- Uses Web Contact Picker API (navigator.contacts.select) to read device contacts.
- Falls back gracefully with a browser support note if not available (desktop/older browsers).
- POST /api/contacts/sync saves contacts; GET /api/contacts/sync returns list + stats.
- Per-contact "Invite" button calls POST /api/referrals/share and optimistically updates status.
- Filter tabs (All / On app / Invited / Pending) driven by stats row.

Performance:
- Dynamic import for map components to avoid SSR issues.
- Images use next/image with fill and sizes for proper optimization.
- Forms use react-hook-form to avoid unnecessary re-renders.


7) End-to-End Flow Examples

Free event commit:
1. User taps "I'm in" on event detail page.
2. UI calls POST /api/events/:id/commit.
3. Server validates JWT, checks capacity, creates EventAttendee in a transaction, increments currentAttendees.
4. Optional NotificationSchedule records are created for reminder dispatch.
5. Response: { status: "committed" }.
6. UI updates button state and shows confirmation.

Paid event commit:
1. Same flow as free event. No payment processing online.
2. Event detail shows a note: "Paid event — arrange payment with the host directly."
3. Ticket is created with paymentStatus: "pending"; QR is available after host confirms payment out of band.

Referral signup:
1. User A sends invite to phone +91XXXXXXXX via POST /api/referrals/share.
2. Twilio sends SMS with link: https://app.com/join?ref=ABCD1234.
3. Contact opens link, /join validates code via GET /api/referrals/:code, marks clicked.
4. Contact taps "Create an account", lands on /signup?ref=ABCD1234.
5. OTP flow proceeds; ref is threaded through verify and password steps.
6. POST /api/auth/register creates user, completes referral: marks invitation "registered", creates Connection(accepted), creates Notifications for both users, increments ReferralLink.registrations.
7. Both users are now connected on first login.

Chat message:
1. User opens chat at /chat/:connectionId.
2. Page connects Socket.io client to room `connection:${connectionId}`.
3. User sends message; POST /api/chat/:connectionId creates Message in DB.
4. API emits "new_message" to the room via globalThis.io.emitToRoom.
5. Both participants receive the message in real time without polling.

Notification dispatch (cron):
1. Vercel Cron calls GET /api/notifications/dispatch every 5 minutes with Bearer CRON_SECRET.
2. Handler queries NotificationSchedules where sendAt <= now and sent = false.
3. For each schedule, sends email (Nodemailer) or SMS (Twilio), creates Notification record, marks schedule sent.


8) DevOps and Deployment

Hosting: Vercel (recommended) for Next.js with cron support, or any Node hosting. PostgreSQL on Supabase. Supabase Storage for media.

Environment variables (all listed in .env.example):
- DATABASE_URL: Supabase PgBouncer pooled connection string
- DIRECT_URL: Supabase direct connection string (used by prisma.config.ts for migrations)
- DATABASE_SSL: true
- EMAIL_ALLOW_SELF_SIGNED: false
- NEXTAUTH_SECRET, NEXTAUTH_URL
- NEXT_PUBLIC_APP_URL
- EMAIL_USER, EMAIL_PASS, EMAIL_FROM (SMTP/Nodemailer)
- RESEND_API_KEY, RESEND_FROM (optional Resend integration)
- TWILIO_ACCOUNT_SID, TWILIO_AUTH_TOKEN, TWILIO_VERIFY_SERVICE_SID
- TWILIO_PHONE_NUMBER, TWILIO_WHATSAPP_NUMBER, DEFAULT_PHONE_COUNTRY_CODE
- NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY
- CRON_SECRET (Vercel Cron authentication)
- NOTIFICATION_DISPATCH_SECRET (manual dispatch authentication)

Migrations:
- prisma.config.ts uses DIRECT_URL (bypasses PgBouncer, required for schema migrations).
- Run: npx prisma migrate deploy in production CI.
- Run: npx prisma generate after schema changes.

Cron:
- vercel.json configures GET /api/notifications/dispatch to run every 5 minutes.
- Can also be triggered manually with the x-notification-secret header.

CI/CD recommended steps:
1. Lint and typecheck (next lint, tsc --noEmit).
2. prisma generate.
3. next build.
4. prisma migrate deploy on production.


9) Performance Optimization

- Event feed capped to 50 results with in-memory scoring (distance + preferences + timing + popularity + connections).
- Connection suggestions and discover endpoints limit result sets.
- Notifications limited to 50 by createdAt desc.
- Media stored in Supabase Storage; DB stores only URLs, no blobs.
- Dynamic import for Leaflet map components prevents SSR hydration issues.
- Prisma select used throughout to avoid over-fetching columns.
- PgBouncer (DATABASE_URL) reduces connection overhead at runtime.

Future caching:
- Redis or edge cache for event feed with short TTL (1-5 minutes).
- CDN caching for public Supabase Storage images.
- Redis adapter for Socket.io to enable multi-node deployment.


10) Security

- SQL injection: Prisma parameterized queries.
- Passwords: bcrypt hashed.
- OTPs: Twilio Verify for phone (server-side), hashed storage for email OTPs.
- Sessions: JWT validated in middleware and API routes.
- Ownership: all mutating endpoints verify the requesting user owns the resource.
- Storage: Supabase service role key used only server-side; client never sees it.
- Cron: dual-auth (NOTIFICATION_DISPATCH_SECRET or Vercel CRON_SECRET Bearer).
- Referral: completion is non-fatal and does not expose user data to the referrer beyond a welcome notification.
- Profile visibility: GET /api/users/:id checks Visibility enum before returning data.

Hardening (recommended):
- Rate limiting per IP/user for OTP and login endpoints.
- Secure HTTP headers (CSP, HSTS, X-Frame-Options, Referrer-Policy) via next.config headers.
- Audit logs for critical actions (deactivate, refund, event delete).


11) Testing Strategy

Recommended unit tests:
- normalizeContact (phone/email normalization edge cases)
- Event scoring function (distance, preferences, timing weights)
- OTP expiry and verification logic

Recommended integration tests:
- Full OTP signup flow (send -> verify -> register)
- Referral flow (invite -> click -> register -> connection created)
- Event commit with capacity enforcement (concurrent requests, 409 on full)
- Ticket refund window enforcement (48h boundary)
- Notification dispatch (schedule created -> dispatch runs -> sent flag set)

Recommended E2E tests:
- Signup and onboarding
- Event create, commit, and scanner validation
- Chat send and receive via Socket.io
- Referral invite and referred-user signup


12) Challenges and Solutions

- Realtime chat in App Router: App Router does not support persistent TCP connections.
  Solution: Bootstrap Socket.io in pages/api/socketio.ts (legacy pages handler) and expose a global emitToRoom helper via globalThis.io so app/api chat routes can emit without HTTP round-trips.

- Referral code lost through multi-step signup: The join page linked to /signup without the ref query param, breaking the referral chain.
  Solution: Thread ref through every URL step (join -> signup -> verify -> password) and include it in the register POST body. Completion is inline in the register handler (non-fatal).

- PgBouncer vs direct for migrations: Prisma cannot run schema migrations through PgBouncer transaction pooler (prepared statements are not supported in that mode).
  Solution: prisma.config.ts exposes directUrl: env('DIRECT_URL') pointing to the direct Supabase connection. Runtime queries use DATABASE_URL (pooled); migrations use DIRECT_URL.

- Notification dispatch without a worker: No separate background process available on Vercel.
  Solution: vercel.json cron calls GET /api/notifications/dispatch every 5 minutes. Dual-auth allows both Vercel-signed requests and manual calls in development.

- Contact sync on desktop browsers: navigator.contacts (Web Contact Picker API) is only available on Chrome Android and Safari iOS 16+.
  Solution: Feature-detect at runtime with "contacts" in navigator; show a descriptive fallback message on unsupported environments rather than crashing.

- Event payments without Stripe: Stripe was removed to reduce scope and PCI complexity.
  Solution: Paid events are committed identically to free events. The event detail page shows a "contact the host directly" note. Tickets track paymentStatus for the host to update manually.


13) Improvements and Future Scope

- Pagination and server-side filtering for event feed (currently in-memory scoring on 50 rows).
- Redis adapter for Socket.io to support horizontal scaling.
- Push notifications: device token registration (stub at /api/notifications/subscribe) and Web Push / FCM delivery.
- Admin and moderation tooling for events and users.
- Event analytics pipeline for host dashboard (attendance rate, swipe-to-commit ratio).
- Group chat (event-scoped rooms, not just 1:1 connections).
- In-app payment request flow (host sends payment link, attendee pays via external provider).
- Redis or edge caching for event feed and connection suggestions.
- Full E2E test suite with Playwright.


14) Interview Questions and Strong Answers

1. Why choose Next.js App Router?
   Unified full-stack routing, SSR/CSR flexibility per route, API colocation, and built-in middleware for auth checks at the edge.

2. How is Socket.io bootstrapped in an App Router project?
   pages/api/socketio.ts is a legacy Next.js handler that supports persistent upgrade connections. The server instance is stored in globalThis.io and accessed from app/api routes via a thin emitToRoom helper in lib/socketServer.ts.

3. How do you prevent duplicate EventAttendee rows?
   A unique index on (eventId, userId) in the database; the Prisma transaction that creates the attendee reads currentAttendees inside the transaction, returns 409 if full, and increments atomically.

4. How do you enforce event capacity under concurrency?
   Prisma transaction wraps the attendee count read and the create; if two requests race, one will fail the capacity check or hit the unique constraint. A 409 is returned to the client.

5. How do you secure OTP flows?
   Email OTPs are hashed before storage. Twilio Verify handles phone OTPs server-side. All tokens have an expiry and a usedAt flag. Resend cooldown is enforced by checking the most recent OTP timestamp.

6. How is chat authorization enforced?
   GET and POST /api/chat/:connectionId query the Connection row to verify the requesting userId is user1Id or user2Id before reading or writing messages.

7. How does the referral flow work end-to-end?
   Invitation is sent via Twilio SMS with a unique referralCode. The /join page validates the code and passes it through the entire signup URL chain. On registration, the register API finds the ContactInvitation by code, updates its status, creates an accepted Connection, notifies both users, and increments ReferralLink stats — all in a non-fatal try/catch so a referral failure never blocks signup.

8. How do you handle event discovery relevance?
   Scoring combines: inverse distance (closer events score higher), preference match (category overlap), time-to-start (events soon score higher), popularity (normalized attendee count), and connection signal (attendees you are connected to).

9. Why use DIRECT_URL and DATABASE_URL separately?
   DATABASE_URL points to the PgBouncer pooler for efficient runtime connection management. DIRECT_URL bypasses the pooler for prisma migrate deploy, which requires persistent connections and prepared statements.

10. How do you scale Socket.io?
    Add a Redis adapter (socket.io-redis) so all Node instances share a pub/sub channel. Combine with sticky sessions or move to a dedicated realtime service.

11. How are images uploaded securely?
    Server-side multipart upload to Supabase Storage using the service role key. The client never touches storage credentials; it only receives the returned public URL.

12. How does notification dispatch work?
    NotificationSchedule rows are created when users commit to events. Vercel Cron calls GET /api/notifications/dispatch every 5 minutes. The handler queries unsent schedules where sendAt <= now, sends email or SMS, creates Notification records, and marks schedules sent.

13. How do you enforce onboarding?
    middleware.ts checks the JWT profileComplete claim. If false and the route is not in the onboarding group, the user is redirected to /onboarding/location.

14. What prevents access by deactivated users?
    JWT includes isDeactivated. Middleware checks it and redirects deactivated users. The claim is refreshed from DB on NextAuth JWT callback.

15. How do you optimize DB queries?
    Limit result sets, use Prisma select to fetch only required fields, rely on indexes for frequent filters (userId, eventId, referralCode, contact), and use PgBouncer for connection pooling.

16. How are refunds handled without Stripe?
    POST /api/tickets/refund validates ownership and a 48-hour cancellation window, sets paymentStatus to "refunded", deletes the EventAttendee, and decrements currentAttendees. The actual money movement is handled out-of-band between host and attendee.

17. How would you add caching?
    Cache the event feed and connection suggestions in Redis with a 1-5 minute TTL keyed by userId+location. Invalidate on new event creation or significant location change.

18. How do you ensure data privacy for user profiles?
    GET /api/users/:id checks the target user's visibility setting: public returns full profile, connections returns profile only if a Connection(accepted) exists between requester and target, private returns 404.

19. How do you prevent race conditions in ticket creation?
    A Prisma transaction reads current capacity and creates the ticket atomically. A unique constraint on qrCode also prevents duplicate tickets.

20. How would you add push notifications?
    Register device tokens at POST /api/notifications/subscribe. Store tokens on the User row. In the dispatch handler, add a Web Push or FCM send alongside the existing email/SMS path. Fall back to in-app Notification row if push fails.


15) Known Bugs Fixed (2026-06-06 Session)

The following bugs were identified and fixed during the 2026-06-06 development session.

--- Environment / Infrastructure ---

1. TLS cert chain rejection (Prisma/pg on Windows)
   Symptom: Prisma threw "Error opening a TLS connection: self-signed certificate in certificate chain" on every DB query.
   Root cause: Supabase's cert chain is not trusted by Node.js built-in TLS on Windows by default.
   Fix: Added DATABASE_SSL_INSECURE=true to .env; lib/prisma.ts reads this flag and sets ssl: { rejectUnauthorized: false } on the pg Pool.

2. Supabase JS SDK fetch failures causing page slowness
   Symptom: All pages took 5-30 seconds to load; server logs showed repeated "TypeError: fetch failed - unable to verify the first certificate".
   Root cause: Supabase JS SDK uses Node.js native fetch for internal REST API calls; same cert trust issue on Windows.
   Fix: Added at the top of next.config.mjs (dev-only guard):
     if (process.env.NODE_ENV !== "production") {
       process.env.NODE_TLS_REJECT_UNAUTHORIZED = "0";
     }
   Result: Warmed-up response times — homepage 120ms, auth session 163ms, events API 755ms.

3. DATABASE_URL / DIRECT_URL had [YOUR-PASSWORD] placeholder
   Symptom: Prisma threw P1000 Authentication failed.
   Fix: Filled in actual Supabase DB password in both connection strings.

--- Middleware ---

4. /notifications and /users/:path* not protected
   Symptom: Unauthenticated users visiting /notifications or /users/[id] saw blank pages instead of being redirected to login.
   Fix: Added both paths to the matcher array in middleware.ts.
   Current matcher:
     "/explore", "/events/:path*", "/connections", "/chat/:path*",
     "/profile/:path*", "/settings/:path*", "/notifications", "/users/:path*", "/onboarding/:path*"

--- Page Bugs ---

5. Privacy settings page always showed "public" on load
   Symptom: /settings/privacy always defaulted to "public" regardless of actual stored value.
   Root cause: Component initialized with useState("public") and never fetched the real value.
   Fix: Added useEffect that fetches GET /api/users/me on mount and sets profileVisibility from data.user.profileVisibility.
   Also added loading state to disable the select and button while fetching.

6. Memories page crash on mount
   Symptom: /profile/memories crashed with "Cannot read properties of undefined (reading 'map')".
   Root cause: Component type defined attendedEvents field, but GET /api/users/me returns privateCalendar.
   Fix: Renamed attendedEvents -> privateCalendar everywhere in the type definition and all JSX usages.

7. Paid event "Accept" button showed "failed"
   Symptom: Clicking "Accept" on a paid event displayed an error state ("failed") because the commit API returns 403 for paid events.
   Root cause: handleCommit called POST /api/events/:id/commit unconditionally; the API intentionally rejects paid events.
   Fix (handleCommit): Added early return for paid events — sets commitStatus to "contact-host" without hitting the API.
   Fix (JSX button): Button label now shows "Contact host to register" when commitStatus === "contact-host"; button is disabled; raw status string is not shown.

8. Chat page missing scroll, Enter key, and message overflow
   Symptom: New messages did not scroll into view; Enter key had no effect; long message threads overflowed the page.
   Fixes:
   - Added messagesEndRef with a useEffect that scrolls it into view on every messages update.
   - Added onKeyDown handler on the input: Enter (without Shift) calls sendMessage().
   - Changed message container from unbounded div to h-96 overflow-y-auto.

9. Chat sendMessage silent failure when Socket.io not initialized
   Symptom: Messages sent via the chat input did not appear until the page was refreshed if the Socket.io globalThis.io was not initialized on the current Node instance.
   Root cause: POST /api/chat/:id saves the message to DB and then calls emitToRoom; if globalThis.io is null the emit silently does nothing and the sender's UI never gets the new message.
   Fix: After a successful POST, sendMessage now calls loadMessages() to re-fetch the thread from DB, guaranteeing the sender sees their message regardless of socket state.

10. Calendar page crash on mount
    Symptom: /profile/calendar crashed with "Cannot read properties of undefined (reading 'some')".
    Root cause: Type CalendarResponse had attendedEvents field; GET /api/users/me returns privateCalendar. Same field name mismatch as the memories page bug (item 6 above) — it was a second occurrence in a different file.
    Fix: Renamed type and all usages (attendedEvents → privateCalendar) in app/(main)/profile/calendar/page.tsx.

11. Tickets page showed QR code as raw text string
    Symptom: /profile/tickets showed "QR: <uuid>" as plain text — not scannable.
    Root cause: Page had no QR rendering; it just printed ticket.qrCode as a string.
    Fix: Installed qrcode.react; replaced raw text with an expandable QRCodeSVG (192×192) per ticket. Each ticket row now has a "Show QR / Hide QR" toggle that expands to the scannable image with a "Show this QR code at the door" label.

--- What was intentionally NOT changed ---
- Paid events still go through the same commit flow (creating EventAttendee) as free events; the "contact host" UI message is cosmetic. Ticket paymentStatus is set to "pending" for the host to resolve out-of-band. This is by design (no online payment processing).
- lib/resend.ts was deleted in a prior session (Resend integration removed). RESEND_API_KEY and RESEND_FROM remain in .env.example as dead vars and are harmless.
- lib/stripe.ts, lib/socket.ts, app/api/tickets/create-intent, app/api/tickets/confirm, app/api/webhooks/stripe were deleted in a prior session when the Stripe integration was removed.


16) Current Project State (2026-06-06, updated after full code audit)

Running:
- Next.js dev server starts cleanly with `npm run dev`.
- Database connection works (Supabase + PgBouncer pooler) with DATABASE_SSL_INSECURE=true.
- All protected routes redirect unauthenticated users to /login.
- Incomplete profiles redirect to /onboarding/profile.
- Deactivated users redirect to /login?deactivated=1.
- qrcode.react installed for client-side QR rendering on the tickets page.
- `npx tsc --noEmit` produces zero TypeScript errors.

Pages confirmed working (all bugs fixed, no runtime crashes):
- /explore — event feed with scoring and swipe view
- /events/[id] — event detail, gallery, map, host card, commit button (free + paid "contact host")
- /events/new — event creation form with map picker
- /events/manage — hosted events list
- /events/manage/[id] — host analytics and management
- /events/manage/[id]/edit — edit form
- /events/manage/[id]/scanner — QR scanner for entry validation
- /connections — accepted connections, suggestions, requests (accept/decline)
- /chat — conversation list with search
- /chat/[id] — message thread with auto-scroll, Enter to send, socket + HTTP fallback
- /notifications — notification list, mark-read, read-all
- /profile — profile overview with stats, calendar grid, attended/hosted/connections sections
- /profile/edit — edit profile photo, bio, preferences
- /profile/calendar — calendar view
- /profile/memories — photo upload, event linking, gallery
- /profile/tickets — ticket list with expandable QR code per ticket
- /settings — settings hub with account deactivation
- /settings/contacts — device contact sync + SMS/WhatsApp invite
- /settings/host — links to manage events and create event
- /settings/notifications — reminders and recommendations toggles
- /settings/payments — explains offline payment arrangement
- /settings/privacy — profile visibility (loads and saves actual stored value)
- /users/[id] — public profile, timeline, hosted events, public calendar, connect/message
- /join?ref=CODE — referral landing page
- /login, /signup, /signup/verify, /signup/password
- /reset, /reset/verify, /reset/password
- /onboarding/profile, /onboarding/location
- /explore/swipe — full-screen swipe mode

Known limitations:
- Socket.io works only in single-Node deployments. Multi-instance deployment requires Redis adapter.
- Web Contact Picker API (/settings/contacts) is only available on Chrome Android and Safari iOS 16+; desktop falls back to a text message.
- Notification push (web push / FCM) is stubbed at POST /api/notifications/subscribe but not fully implemented.
- Event feed scoring is in-memory on top 50 rows; no pagination.

Security hardening applied (see section 17):
- Rate limiting on OTP send, OTP verify, register, and login endpoints.
- CSRF origin checks on auth routes and event creation/swipe endpoints.
- HTTP security headers (X-Content-Type-Options, X-Frame-Options, HSTS, Referrer-Policy, Permissions-Policy).
- Strict file upload validation (MIME + extension whitelist, 10 MB cap).

UX improvements applied (see section 18):
- Explore empty state with welcome banner and CTA.
- NotificationBell polls every 30 seconds.
- Chat list: clearer section headers, CTA when no connections.
- Ticket payment status badge (pending/paid/refunded/failed).
- Event delete inline confirmation with attendee count.
- Cancel attendance button on event detail page.
- Full-screen swipe mode at /explore/swipe.
- Onboarding order fixed: profile → location.

Full code audit bugs fixed (see section 20):
- Event delete FK violation on NotificationSchedule.
- Notification read IDOR.
- Event feed exposing non-public events.
- Duplicate EventSwipe rows.
- QR code re-scanning.
- Dead /api/auth/login route removed.
- prisma.config.ts TypeScript error resolved.
- CalendarGrid day-header alignment fixed.
- CSRF protection added to POST /api/events and POST /api/events/swipe.

Remaining future work:
- Full push notification delivery (register device tokens, deliver via Web Push or FCM).
- Redis adapter for Socket.io to support horizontal scaling.
- Pagination and server-side filtering for event feed.
- Admin / moderation tooling.
- Full E2E test suite (Playwright).
- CSP (Content-Security-Policy) header (omitted to avoid breaking inline styles/Leaflet).


17) Security Hardening (2026-06-06 Session)

The following security issues were identified and fixed.

1. No rate limiting on auth endpoints
   Risk: Unlimited OTP sends, OTP guesses, login attempts = brute-force attack surface.
   Fix: Created lib/rateLimit.ts — in-memory fixed-window rate limiter.
   Applied to:
   - POST /api/auth/send-otp: 5 requests / 15 minutes per IP
   - POST /api/auth/verify-otp: 15 requests / 15 minutes per IP (on top of existing 5-attempt DB lock)
   - POST /api/auth/register: 5 requests / 1 hour per IP
   - authorize() in authOptions.ts: 10 login attempts / 15 minutes per IP

2. No CSRF protection
   Risk: A malicious page could trigger state-changing API calls from a victim's browser session.
   Fix: Created lib/csrf.ts — compares the Origin header against NEXTAUTH_URL/NEXT_PUBLIC_APP_URL.
   Applied to all CSRF-sensitive POST endpoints in auth routes.

3. Missing HTTP security headers
   Fix: Added to next.config.mjs via headers() applied to /:path*:
   - X-Content-Type-Options: nosniff
   - X-Frame-Options: SAMEORIGIN
   - Referrer-Policy: strict-origin-when-cross-origin
   - Permissions-Policy: camera=(), microphone=(), geolocation=(self), payment=()
   - Strict-Transport-Security: max-age=31536000; includeSubDomains

4. Weak file upload validation
   Risk: An attacker could upload a PHP/JS file disguised as an image.
   Fix: Added to POST /api/storage/upload:
   - 10 MB size cap
   - MIME type whitelist: jpeg, png, gif, webp, avif, heic, heif
   - File extension whitelist: same set
   Both checks must pass; either failure returns 400.

5. Map container crash (Leaflet HMR)
   Symptom: "Map container is already initialized" error on hot reloads in /events/[id].
   Root cause: HMR reuses DOM nodes but Leaflet's _leaflet_id persists; react-leaflet's MapContainer cannot detect this.
   Fix: Rewrote EventMap and MapPicker to use raw L.map() inside useEffect instead of react-leaflet's MapContainer.
   The effect explicitly deletes node._leaflet_id before creating a new L.map instance.


18) UX / Missing Flow Fixes (2026-06-06 Session)

The following UX gaps and missing flows were identified and implemented.

1. Explore page empty state
   Problem: New users with no events nearby saw a blank card with no explanation.
   Fix: Added a welcome banner ("Welcome to OuttaCouch!") when events.length === 0 and no search query.
   Banner explains the product and includes a "Host your own event" CTA button.
   Empty search now shows "No events match your search." instead of generic "No events found."

2. NotificationBell: no real-time update
   Problem: New connection requests and notifications only appeared after page refresh.
   Fix: Added a 30-second polling interval (setInterval / clearInterval) to NotificationBell.tsx so the
   unread badge refreshes without a reload. Notification DB records are already created by existing API routes.

3. Chat list UX
   Problem: "Connections" section header was unclear; no CTA when a user has no connections.
   Fix: Renamed section header to "Start a chat" with descriptive subtext.
   When no connections: shows an info box with a "Find people to connect with" button linking to /connections.
   When connections exist but no chats: shows "tap a connection above to say hello" hint text.

4. Ticket status badge
   Problem: Tickets listed on /profile/tickets showed quantity and price but no payment status indicator.
   Fix:
   - Added paymentStatus to GET /api/tickets/me response.
   - Added PaymentBadge component in the tickets page: colored chips
     (paid=green, pending=yellow, refunded=blue, failed=red) shown inline next to event title.

5. Event delete confirmation
   Problem: "Delete event" used window.confirm() — a browser-native dialog with no context about impact.
   Fix: Replaced with inline confirmation UI in the host dashboard.
   Clicking "Delete event" reveals an inline warning showing the attendee count (e.g. "3 attendees will be affected").
   Two buttons: "Yes, delete" and "Cancel". Confirmation state resets on cancel or error.

6. Cancel attendance
   Problem: Users who committed to a free event had no way to cancel their attendance.
   Fix:
   - Added DELETE /api/events/[id]/commit — removes EventAttendee, decrements currentAttendees.
   - Updated GET /api/events/[id] to return isCommitted: boolean (true if requesting user has an attendee record).
   - Event detail page initializes commitStatus to "committed" if isCommitted is true (so return visits show the right state).
   - Added "Cancel attendance" link below the confirmed button (only for free events, only when committed).

7. Dedicated swipe mode page
   Problem: The swipe interface was embedded inside the explore page with no immersive option.
   Fix: Created /explore/swipe — a standalone full-screen swipe page.
   - No PageShell nav overhead; minimal header with a "← Back" link.
   - Full-screen centered SwipeStack with empty-state handling.
   - Added "Full-screen swipe" button in the explore page's section card header.
   - SectionCard component updated to accept optional headerAction prop.

8. Onboarding order: profile before location
   Problem: New users were sent to /onboarding/location first, then /onboarding/profile — but
   setting a name/photo before asking for location makes more sense UX-wise.
   Fix:
   - signup/password PasswordClient.tsx: after registration and sign-in, redirects to /onboarding/profile.
   - /onboarding/profile: saves name/photo/bio/preferences via PUT /api/users/me (no profileComplete flag);
     redirects to /onboarding/location; button label updated to "Next: set location".
   - /onboarding/location: after saving location, sets profileComplete: true (via location API update +
     session update), redirects to /explore.
   - PUT /api/users/me/location: now accepts optional profileComplete: true in the request body.
   New onboarding flow: Register → Profile (name/photo/bio) → Location → Explore.


19) Elevator Pitch

OUTTACOUCH is an event-first social platform that uses real attendance to drive meaningful connections.
Users discover nearby events, commit to attend, and automatically connect with people they meet in person.
Hosts create and manage events with tools for attendee tracking, QR check-in, and gallery management.
Friends are invited via phone contacts using a referral system — when they sign up through the link, they are instantly connected to the person who invited them.
The system is built on Next.js 15 with Prisma and PostgreSQL, Supabase for media storage, Twilio for OTP and SMS invitations, Socket.io for realtime chat, and Vercel Cron for notification dispatch.
The focus is on relevance, real-world trust, and a clean user experience that moves people from browsing to actually showing up.


20) Full Code Audit (2026-06-06, Continuation Session)

A file-by-file audit of every route, page, component, and config file was performed. Nine bugs were identified and fixed.

--- Data Integrity ---

1. Event delete: FK violation on NotificationSchedule
   File: app/api/events/[id]/route.ts — DELETE handler
   Symptom: Deleting an event with pending notification schedules threw a PostgreSQL FK constraint violation.
   Root cause: NotificationSchedule.eventId is a non-nullable FK to Event with no onDelete clause (defaults to RESTRICT in PostgreSQL). The delete transaction did not clean up NotificationSchedule rows before deleting the Event row.
   Fix: Added prisma.notificationSchedule.deleteMany({ where: { eventId: event.id } }) to the $transaction array, before the event.delete() call.

2. Event feed: non-public events visible to everyone
   File: app/api/events/route.ts — GET handler
   Symptom: The event feed returned events with visibility "connections" or "private" to all callers, including unauthenticated users.
   Root cause: No visibility filter on the Prisma findMany or count() calls.
   Fix: Added where: { visibility: "public" } to both prisma.event.findMany and prisma.event.count.

3. Duplicate EventSwipe rows
   File: app/api/events/swipe/route.ts — POST handler
   Symptom: Every swipe call created a new row, regardless of whether the user had already swiped the same event. Repeated swipes accumulated duplicate rows.
   Root cause: Handler always called prisma.eventSwipe.create without checking for an existing record.
   Fix: Replaced unconditional create with a findFirst + conditional update-or-create pattern.

4. QR code re-scanning (ticket re-entry)
   File: app/api/tickets/validate/route.ts — POST handler
   Symptom: Scanning an already-validated QR code succeeded and returned "validated" again, allowing re-entry.
   Root cause: No check on the qrValidated field before performing the update.
   Fix: Added guard — if ticket.qrValidated is true, return 409 { error: "Ticket already validated" } immediately.

--- Security ---

5. Notification read IDOR
   File: app/api/notifications/[id]/read/route.ts — PUT handler
   Symptom: Any authenticated user could mark any other user's notification as read by supplying an arbitrary notification ID.
   Root cause: Handler called prisma.notification.update without verifying the notification belonged to the requesting user.
   Fix: Added prisma.notification.findUnique before the update; return 404 if not found or if notification.userId !== token.sub.

6. Dead /api/auth/login route
   File: app/api/auth/login/route.ts (deleted)
   Symptom: Standalone POST /api/auth/login route existed but was never called by the frontend (frontend uses NextAuth signIn). It created no session, had no CSRF protection, and had no rate limiting.
   Risk: The route was reachable dead code with no security controls — a passive attack surface with no business value.
   Fix: Deleted app/api/auth/login/route.ts and its parent directory.

7. CSRF protection missing on event creation and event swipe
   Files: app/api/events/route.ts (POST), app/api/events/swipe/route.ts (POST)
   Symptom: Both endpoints accepted cross-origin requests with a valid session cookie.
   Root cause: isSameOrigin check from lib/csrf.ts was applied to auth routes but not to these two mutation endpoints.
   Fix: Added import { isSameOrigin } from "@/lib/csrf" and an early-return guard returning 403 at the top of both POST handlers.

--- Infrastructure / TypeScript ---

8. prisma.config.ts TypeScript error
   File: prisma.config.ts
   Symptom: npx tsc --noEmit reported "Object literal may only specify known properties, and 'directUrl' does not exist in type '{ url?: string | undefined; shadowDatabaseUrl?: string | undefined; }'".
   Root cause: directUrl is not a valid property in the @prisma/config defineConfig datasource type in Prisma 7. The property had been added under the assumption it matched the schema.prisma datasource block syntax, but the TypeScript config API does not accept it.
   Fix: Removed the directUrl line from prisma.config.ts. The DIRECT_URL env var is still referenced in .env and used by prisma migrate deploy via the schema.prisma datasource block (not the TypeScript config).
   Result: tsc --noEmit now exits with zero errors.

--- UI ---

9. CalendarGrid: day-header row outside the inner grid container
   File: components/profile/CalendarGrid.tsx
   Symptom: The "Sun Mon Tue Wed Thu Fri Sat" day-header row was rendered in a grid grid-cols-7 that was a sibling of the inner rounded-2xl border card, not a child. The header grid and the day-button grid were two separate elements with separate padding, so column alignment broke visually.
   Root cause: The day-header div was placed between the outer container and the inner card instead of inside the inner card before the days grid.
   Fix: Moved the day-header grid inside the inner rounded-2xl border container, directly above the day-button grid. Also removed the duplicate month/year heading that existed only inside the inner card (the outer container already displayed it). Both grids now share the same padding context and column alignment is correct.

--- Post-audit state ---
- npx tsc --noEmit: zero errors.
- All 9 bugs resolved.
- No breaking changes to API contracts or DB schema.
